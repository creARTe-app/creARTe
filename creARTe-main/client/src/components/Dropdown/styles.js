import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    dashboard: {
        color: '#fafeff',
        backgroundColor: '#66806E',
    },
    logout: {
        color: 'red',
    },
    options: {
        color: '#0C2D48',
    },
    adminOption: {
        color: '#0C2D48',
    },
}))